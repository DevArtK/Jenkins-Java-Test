pipeline {
    agent any

    stages {

        stage('Setup') {
            steps {
                echo '----- Cloning from VCS -----'
            }
        }

        stage('Build') {
            steps {
                echo '----- Building Project -----'
            }
        }

        stage('Test') {
            steps {
                echo '----- Running Tests -----'
            }
        }

    }
}
